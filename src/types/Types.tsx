export interface Type {
    id: number,
    name: string,
    badge: string,
    description: string,
    category: string,
    level: string,
    rating: number,
    icon?: string,
    image:string
}